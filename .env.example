# Copy this to .env and fill the real secret values. Never commit real secrets to source control.

# Server port
PORT=5000

# MongoDB connection string
# Replace <username>, <password>, and <dbname> with real values.
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.s8yaihg.mongodb.net/<dbname>?retryWrites=true&w=majority&appName=Cluster0

# Strong JWT secret used to sign auth tokens (choose a long random string)
JWT_SECRET=replace_with_strong_random_value
JWT_EXPIRES_IN=1h

# CORS origin (frontend URL). Set to your frontend host in production.
CORS_ORIGIN=http://localhost:5500

# Optional admin account used by seed script (only if you want an automatic admin)
# If you prefer, create admin user manually in the DB instead of using these.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!

# Environment
NODE_ENV=development

# Logging level
LOG_LEVEL=info
